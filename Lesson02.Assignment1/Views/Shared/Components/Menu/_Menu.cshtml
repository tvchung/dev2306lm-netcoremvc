@model IEnumerable<Lesson02.Assignment1.Models.Category>

<ul class="list-group">
    <li class="list-group-item active" aria-current="true">DANH MỤC</li>

    @{
        foreach (var item in Model)
        {
            <li class="list-group-item">
                <a href="/san-pham/@item.Id">@item.Name</a>
            </li>
        }
    }

</ul>